#!/usr/bin/env node

import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const bgTypesPath = path.join(__dirname, '..', 'pkg', 'wasabi_table_bg.d.ts');

const contents = `/* Auto-generated by scripts/patch-wasm-types.mjs — do not edit */
export function __wbg_set_wasm(val: WebAssembly.Exports): void;
`;

fs.writeFileSync(bgTypesPath, contents, 'utf8');
console.log('✅ Patched WASM bg types:', path.relative(process.cwd(), bgTypesPath));
